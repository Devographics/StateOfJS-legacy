import React, { Component } from 'react'

export default class OpinionsSelector extends Component {
    render() {
        return (
            <select className="OpinionSelector">
                <option value="would_use">used and would use again ▻</option>
            </select>
        )
    }
}
